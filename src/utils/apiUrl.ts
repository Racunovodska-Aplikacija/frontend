/**
 * Get the API base URL
 * Uses relative path for local development (relies on Next.js rewrites)
 * Falls back to environment variable if needed
 */
export const getApiUrl = (): string => {
    // Use relative path (for local development with Next.js rewrites)
    return "";
};
